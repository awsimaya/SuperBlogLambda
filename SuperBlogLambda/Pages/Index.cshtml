@page
@model IndexModel

@{
    ViewData["Title"] = "Home page";
}

@{
    
    foreach (var post in Model.Posts)
    {
        <h2 style="color:darkcyan"> @post.postTitle</h2>
        <label style="color:cadetblue">@post.postTime UTC</label>
        <p>@post.postContent</p>
        <hr />
    }
}
